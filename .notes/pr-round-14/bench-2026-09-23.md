# Bench run 2026-09-23 — his three behavioural commits, and two findings

## Results

| commit | result |
|---|---|
| `62b60e2b` menu cursor | **PASS.** Cursor stays where it was left across Info and Back. |
| `0c5a7d69` gen1_attempted | **PASS.** On `slix-1k-coin18`, card lifted during the consent screen: "Card removed before the write could finish." The `gen1_spent` branch did NOT fire, so the false "56/57/62/63 may differ" is gone. |
| our consent fix | **PASS, end to end.** Consent on card A, remove, Retry with card B on the coil: the opt-in appears for B. Before, B took the gen1 write unasked. |
| `4d20f06e` CFG clamp | Not run. Deterministic from source, only bites on a hand-edited source, and it is the one test that programs a geometry into the only usable gen2 card. |

`749f10e6` needed no bench; verified against the firmware source.

## FINDING — Retry after a gen1 opt-in re-runs gen1 with no consent screen

`iso15693_force_gen1` is set true in exactly one place, the opt-in (`gen1_optin.c:93`), and cleared
in exactly one place, the ISO15693 menu (`iso15693.c:63` and `:73`).

Retry on the write-fail screen does `scene_manager_previous_scene`, which returns to
`NfcMagicSceneWrite`, which re-runs the write on enter reading `iso15693_force_gen1` -- still true.
So **any route back into the write scene that does not pass through the menu repeats the destructive
gen1 write without re-consent.**

Noticed at the bench as "the second write no longer gives me the gen1 warning, it just writes".

Why it is worth more than it first looks: Retry does not re-identify the card. Consent was given for
the card that was present. A Retry after CardLost -- the case Retry exists for -- can land on a
DIFFERENT card, and that one gets four blocks written having never been offered the screen.

Not previously recorded anywhere. Sits with the gen1 caveat gate in the addressing work, since both
are about what the gen1 path is entitled to claim and do.

## Re-confirmed, already recorded

The 28-block gen1 clone reports Partial with "gen1: 56/57/62/63 differ" although nothing was skipped
and nothing failed. Identical to the 2026-09-13 measurement in
[pr-round-10/unaddressed-write-finding.md](../pr-round-10/unaddressed-write-finding.md). Reproduces.

## RAISED WITH HIM, deliberately not fixed here

The protocol menus keep their cursor across a whole new card scan, not just within one. Hit twice at
the bench: write UID, success returns to the main menu, Check Magic Tag, More -- and the cursor is
still on Write UID rather than the menu's first item.

Three things decided it:

- **It is app-wide.** All six protocol menus set scene state on event, read it on enter and never
  clear it. ISO15693 is not the odd one out, so fixing only it would make it one.
- **There is exactly one place it would go.** `magic_info.c` dispatches every protocol menu after a
  scan -- eight `next_scene` calls in one function -- so the fix is about six lines, and it is all
  six menus or none.
- **The app is already inconsistent with itself.** The scene state is only ever written by the user's
  own selection, so on first load it is 0 and the first scan lands on index 0. Every later scan lands
  on stale state. Same action, different cursor, depending only on whether it is the first scan of
  the session.

That last point is the argument, and it is mfcarroll's rather than mine. My objection had been that
index 0 is `Write` in all six menus, so resetting would put the cursor on the destructive item --
but it already does that on the first scan, so resetting adds no exposure and simply makes scans two
onwards behave like scan one. Nor does it cut against `62b60e2b`, which is about returning from Info
within one flow: a different axis.

Not fixed here only because it is shared app code in a PR about ISO15693. Raised in the round-14
reply.
