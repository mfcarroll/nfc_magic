Fixed in `9c404bc4`. A `uid_verified` flag, false on both no-observation branches, rendered as "UID not re-checked." on the wipe screen's third line. Your argument for not raising an error is untouched — only the silence changes.

I took your "keeps the success tone if you want it" and kept it, for this case only: the wipe itself finished, and only a best-effort check did not run. A sweep the clock cut short falls on the other side of that line and is now `Partial`. The reasoning for the split is in the top-level reply, since it also answers your `poller.h:24` note.

Your `:1194` observation that narrowing the activation budget makes this branch easier to hit is right and worth keeping in view. It argued for saying something rather than for widening the budget, and that is what changed.

A cut sweep also gets the Retry/Exit buttons a lost card already had, per your `write_fail.c:307` note — it is the one wipe outcome where re-running is the correct action.
