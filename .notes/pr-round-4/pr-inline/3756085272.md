Fixed in `e4af5397` — and it found a bug of ours, so thank you for not treating it as a wording nit.

Both card-lost paths now `break` rather than `return`, so the tail runs and the line is emitted. The caller discards the counters when the card is gone, so the tail arithmetic running for it is harmless.

Then the log immediately showed the change was wrong: a wipe lifted mid-sweep reported "71 blocks attempted, 64 cleared" having attempted 72. The loop variable is the attempted count at every other exit — the capacity break increments before breaking, the deadline breaks before attempting, a full run ends past the last index — and these two break *after* attempting a block without incrementing. Fixed in `4884d067`.

The tell was the gap. The run that trips the presence check is eight blocks, so attempted minus cleared has to be eight on that path, and it read seven twice before and eight on all three runs after.
