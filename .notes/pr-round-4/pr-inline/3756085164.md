Fixed in `9c404bc4`. You're right that this is the card the clock exists for and the one screen that never saw the flag — `wiped == 0` short-circuits here before any of the truncation reporting.

Your `Stopped: time limit` and the partial screen's `at N of M` turn out to want different homes, and the line has room for both:

```
No block accepted the
zero-write. Time limit
stopped at 40 of 256.
```

The reason matters more here than on the cut-sweep screen. There, blocks were cleared, so the extent is the actionable half — everything above the cut may still hold data. Here nothing was cleared either way, so the extent changes nothing about what survived; what the user cannot otherwise work out is why the app tried 40 of a claimed 256 rather than stopping at the card's edge. Without the reason it reads as the app giving up arbitrarily.

The partial screen's own qualifier moved rather than being fixed in place: a truncated sweep now reports `Partial` from the poller and carries its own reason code, so it has a screen that can show both figures without competing for the qualifier slot. Reasoning in the top-level reply.
